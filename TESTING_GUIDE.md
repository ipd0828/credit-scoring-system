# Руководство по тестированию проекта кредитного скоринга

## 📋 Обзор

Проект включает комплексную систему тестирования с unit, интеграционными и end-to-end тестами, а также автоматизированными проверками качества кода.

## 🏗️ Структура тестов

```
tests/
├── conftest.py              # Общие фикстуры
├── unit/                    # Unit тесты
│   ├── test_eda.py         # Тесты EDA модуля
│   ├── test_preprocessing.py # Тесты предобработки
│   ├── test_models.py      # Тесты обучения моделей
│   ├── test_monitoring.py  # Тесты мониторинга
│   └── test_pipeline.py    # Тесты главного пайплайна
├── integration/             # Интеграционные тесты
│   └── test_full_pipeline.py # Тесты полного пайплайна
└── e2e/                     # End-to-end тесты
    └── test_end_to_end.py  # E2E тесты
```

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# Установка всех зависимостей
make install

# Или вручную
pip install -r requirements.txt
```

### Запуск тестов

```bash
# Все тесты
make test-all

# Только unit тесты
make test-unit

# Только интеграционные тесты
make test-integration

# Только E2E тесты
make test-e2e

# Быстрые тесты
make test-fast
```

## 🔧 Конфигурация

### pytest.ini

Проект использует pytest с настройками в `pytest.ini`:

- **Покрытие кода**: минимум 70%
- **Маркеры**: unit, integration, e2e, slow, fast, ml, data, monitoring
- **Отчеты**: HTML, XML, терминал
- **Фильтры предупреждений**: настроены для ML библиотек

### Makefile

Включены удобные команды для всех операций:

```bash
# Тестирование
make test-all         # Все тесты
make test-unit        # Unit тесты
make test-integration # Интеграционные тесты
make test-e2e         # E2E тесты
make test-fast        # Быстрые тесты
make test-slow        # Медленные тесты
make test-ml          # ML тесты
make test-data        # Тесты данных
make test-monitoring  # Тесты мониторинга

# Качество кода
make format           # Форматирование
make lint             # Линтинг
make quality-check    # Полная проверка
make ci               # CI пайплайн

# Очистка
make clean            # Очистка временных файлов
make clean-reports    # Очистка отчетов
```

## 📊 Типы тестов

### 1. Unit тесты

Тестируют отдельные функции и классы:

```bash
# Все unit тесты
pytest tests/unit/ -v

# Конкретный модуль
pytest tests/unit/test_eda.py -v

# С покрытием
pytest tests/unit/ --cov=scripts --cov-report=html
```

**Покрываемые модули:**
- `test_eda.py` - EDA функции
- `test_preprocessing.py` - предобработка данных
- `test_models.py` - обучение моделей
- `test_monitoring.py` - мониторинг
- `test_pipeline.py` - главный пайплайн

### 2. Интеграционные тесты

Тестируют взаимодействие между модулями:

```bash
# Интеграционные тесты
pytest tests/integration/ -v

# С маркерами
pytest tests/ -m integration -v
```

**Тестируемые сценарии:**
- Полный пайплайн EDA → Preprocessing → Training
- Поток данных между модулями
- Совместимость моделей с данными

### 3. End-to-end тесты

Тестируют полный пользовательский сценарий:

```bash
# E2E тесты
pytest tests/e2e/ -v

# С маркерами
pytest tests/ -m e2e -v
```

**Тестируемые сценарии:**
- Полное выполнение пайплайна
- Обработка ошибок и восстановление
- Производительность и использование памяти

## 🎯 Маркеры тестов

Используйте маркеры для категоризации тестов:

```python
@pytest.mark.unit
def test_eda_function():
    pass

@pytest.mark.integration
def test_pipeline_integration():
    pass

@pytest.mark.e2e
def test_full_pipeline():
    pass

@pytest.mark.slow
def test_large_dataset():
    pass

@pytest.mark.ml
def test_model_training():
    pass
```

### Запуск по маркерам

```bash
# Быстрые тесты
pytest -m fast -v

# Медленные тесты
pytest -m slow -v

# ML тесты
pytest -m ml -v

# Тесты данных
pytest -m data -v

# Тесты мониторинга
pytest -m monitoring -v
```

## 📈 Покрытие кода

### Настройка покрытия

```bash
# Запуск с покрытием
pytest --cov=scripts --cov-report=html --cov-report=term-missing

# Минимальное покрытие 70%
pytest --cov=scripts --cov-fail-under=70

# Исключение файлов
pytest --cov=scripts --cov-omit="*/tests/*,*/venv/*"
```

### Отчеты покрытия

- **HTML**: `htmlcov/index.html`
- **XML**: `coverage.xml`
- **Терминал**: отображается в консоли

## 🔍 Качество кода

### Линтеры

```bash
# Flake8 (стиль кода)
flake8 scripts/ tests/

# MyPy (проверка типов)
mypy scripts/ --ignore-missing-imports

# Bandit (безопасность)
bandit -r scripts/ -f json -o bandit-report.json

# Все сразу
make lint
```

### Форматирование

```bash
# Black (форматирование)
black scripts/ tests/

# isort (сортировка импортов)
isort scripts/ tests/

# Все сразу
make format
```

### Pre-commit hooks

```bash
# Установка
make pre-commit-install

# Ручной запуск
make pre-commit-run

# Обновление
make pre-commit-update
```

## 🐳 Docker тестирование

### Тестирование в Docker

```bash
# Сборка образа
make docker-build

# Запуск тестов в Docker
docker run --rm -v $(pwd):/app credit-scoring-ml pytest tests/unit/ -v

# Интерактивный режим
make docker-run-interactive
```

## 📊 Производительность

### Бенчмарки

```bash
# Профилирование пайплайна
make profile-pipeline

# Профилирование тестов
make profile-test

# Бенчмарк производительности
make benchmark
```

### Мониторинг ресурсов

```bash
# Тесты с мониторингом памяти
pytest tests/e2e/test_end_to_end.py::TestEndToEndPipeline::test_memory_usage -v

# Тесты производительности
pytest tests/e2e/test_end_to_end.py::TestEndToEndPipeline::test_performance_benchmarks -v
```

## 🚨 Обработка ошибок

### Тестирование ошибок

```python
def test_error_handling():
    with pytest.raises(ValueError):
        function_that_raises_error()
    
    with pytest.raises(FileNotFoundError):
        load_nonexistent_file()
```

### Моки и заглушки

```python
@patch('scripts.model_training.mlflow_tracking.setup_mlflow_experiment')
def test_with_mock(mock_mlflow):
    mock_mlflow.return_value = MagicMock()
    # Тест с моком
```

## 📝 Логирование

### Настройка логирования в тестах

```python
import logging

def test_with_logging(caplog):
    with caplog.at_level(logging.INFO):
        function_that_logs()
    
    assert "Expected log message" in caplog.text
```

## 🔄 CI/CD интеграция

### GitHub Actions

Тесты автоматически запускаются в CI/CD пайплайне:

```yaml
# .github/workflows/ci-cd.yml
- name: Run unit tests
  run: pytest tests/unit/ -v --cov=scripts --cov-report=xml

- name: Run integration tests
  run: pytest tests/integration/ -v

- name: Run E2E tests
  run: pytest tests/e2e/ -v
```

### Локальный CI

```bash
# Полный CI пайплайн
make ci

# Быстрый CI
make ci-fast
```

## 📋 Чек-лист тестирования

### Перед коммитом

- [ ] `make test-fast` - быстрые тесты
- [ ] `make format` - форматирование кода
- [ ] `make lint` - проверка линтерами
- [ ] `make pre-commit-run` - pre-commit hooks

### Перед push

- [ ] `make test-all` - все тесты
- [ ] `make quality-check` - полная проверка качества
- [ ] `make ci` - CI пайплайн

### Перед релизом

- [ ] `make test-e2e` - E2E тесты
- [ ] `make security-check` - проверка безопасности
- [ ] `make report-coverage` - отчет о покрытии
- [ ] `make report-quality` - отчет о качестве

## 🛠️ Отладка тестов

### Запуск с отладкой

```bash
# Отладка конкретного теста
pytest tests/unit/test_eda.py::test_load_data -v --pdb

# Отладка с подробным выводом
pytest tests/unit/test_eda.py -v -s --tb=long

# Отладка пайплайна
make debug-pipeline
```

### Полезные опции pytest

```bash
# Остановка на первой ошибке
pytest -x

# Запуск только упавших тестов
pytest --lf

# Запуск с максимальным количеством ошибок
pytest --maxfail=3

# Параллельный запуск
pytest -n auto
```

## 📚 Дополнительные ресурсы

### Документация

- [pytest документация](https://docs.pytest.org/)
- [pytest-cov документация](https://pytest-cov.readthedocs.io/)
- [coverage.py документация](https://coverage.readthedocs.io/)

### Лучшие практики

1. **Именование тестов**: `test_<function_name>_<scenario>`
2. **Один тест - одна проверка**
3. **Используйте фикстуры для общих данных**
4. **Мокайте внешние зависимости**
5. **Тестируйте граничные случаи**
6. **Документируйте сложные тесты**

### Примеры тестов

См. файлы в `tests/unit/` для примеров:
- Тестирование функций EDA
- Тестирование предобработки данных
- Тестирование обучения моделей
- Тестирование мониторинга
- Тестирование пайплайна

---

**Примечание**: Этот проект использует современные практики тестирования ML проектов с акцентом на воспроизводимость, покрытие кода и автоматизацию.
